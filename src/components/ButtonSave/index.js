import React from 'react';

const ButtonSave = () => {
  return (
    <button
      className="mt-4 bg-gray-300 px-3 py-2 rounded-md text-gray-900 font-semibold hover:text-gray-100 drop-shadow-lg hover:scale-105 active:scale-95 ease-in-out duration-100"
      type="submit"
    >
      Salvar
    </button>
  );
};

export default ButtonSave;
